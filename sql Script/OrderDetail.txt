CREATE TABLE OrderDetail (
    Oid INT,
    Pid INT,
    quantity INT,
    price DECIMAL(10, 2),
    PRIMARY KEY (Oid, Pid),
    FOREIGN KEY (Oid) REFERENCES `Order`(Oid),
    FOREIGN KEY (Pid) REFERENCES Product(Pid)
);
